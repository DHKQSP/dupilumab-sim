# SPEC.md — 듀필루맙 바이오시밀러 Phase 1 PK 시뮬레이션 기준 문서

> 모든 세션은 이 문서를 먼저 읽는다. 파라미터·가정을 바꾸면 이 문서와 `DECISIONS.md`를 같은 커밋에서 갱신한다.
> 상태 표기: `[확정]` 출처 확인 완료 / `[PENDING]` 사용자 입력 대기 / `[DEV-가정]` 개발용 임시값(최종 실행 금지).

## 0. 목적

듀필루맙 바이오시밀러 Phase 1 comparative PK study(단회 피하투여, 평행군)에서
**AUC0-last를 1차 평가변수로 두는 것이 AUC0-inf 대비 정보 손실이 없음**을
시뮬레이션으로 보인다. 산출물은 규제 제출용 보조 분석 수준의 재현 가능한 코드와 결과 보고서다.

핵심 전제: 비구획분석(NCA)과 90% 신뢰구간 판정은 실제 시험과 동일한 λz 선택 규칙을 쓰는
기존 **BEmaster** 모듈을 버전 고정 의존성으로 호출한다. 이 저장소는 NCA·BE 판정 규칙을
자체 구현하지 않는다(§6).

## 1. 진행 원칙

1. 단계별 진행. 각 단계의 자동 테스트를 통과하기 전에는 다음 단계의 *결과*를 보고하지 않는다.
   (코드 골격은 미리 만들 수 있으나 결과 보고는 금지.)
2. 파라미터 값은 출처(문헌·표·쪽수)를 주석과 config에 남긴다. 출처가 불명확한 값은
   임의로 채우지 않고 `[PENDING]`으로 두고 질문한다.
3. 검증 목표에 맞추려는 파라미터 임의 조정 금지. 미달 시 어떤 가정이 안 맞는지 보고한다.
4. 커밋은 PR 기반. 난수 시드, 패키지 버전(`renv.lock`), 세션 정보, 실행 로그를 남긴다.

## 2. 환경

| 항목 | 값 | 비고 |
|---|---|---|
| R | 4.3.3 (Ubuntu 24.04 apt `r-base`) | 이 세션의 실행 환경은 CRAN 접근이 차단되어 있어 apt + GitHub `cran/*` 미러(태그 = CRAN 릴리스)에서 설치 |
| rxode2 | 5.1.7 (CRAN 릴리스, `cran/rxode2@5.1.7`) | ODE 시뮬레이션 |
| lotri / dparser / PreciseSums / rxode2ll | 1.0.5 / 1.3.1-13 / 0.7 / 2.0.18 | rxode2 의존성 |
| Rcpp / BH / RcppParallel | 1.1.2 / 1.90.0-1 / 6.2.1 | apt 판(Rcpp 1.0.12, Debian BH)은 rxode2 5.1.7 빌드에 부족하여 CRAN 릴리스로 교체 |
| renv | 1.2.4 | 잠금 파일 `renv.lock` |
| testthat | 3.2.1 | 자동 테스트 |
| BEmaster | **[PENDING]** 버전·입수 경로 미확정 | GitHub `DHKQSP/BE-Master`는 빈 저장소(커밋 없음). §6 참조 |
| 저장소 | `main` 브랜치 없음(빈 저장소로 생성됨) | PR을 열려면 기본 브랜치가 필요 — 사용자 조치 필요(Q10) |

재현 방법은 `README.md` §재현 참조.

## 3. 모델

### 3.1 구조 `[확정]`
2구획, 1차 흡수, 선형 소실과 Michaelis–Menten(MM) 소실의 병렬 구조.
상태변수는 양(mg): `depot`, `central`, `periph`. 농도 `C = central / Vc` (mg/L). 시간 단위 day.

```
d/dt(depot)   = -ka * depot
d/dt(central) =  ka * depot - (ke + k12) * central + k21 * periph - Vmax * C / (Km + C) * Vc
d/dt(periph)  =  k12 * central - k21 * periph
depot(0) = F * Dose
```
`Vmax`는 mg/L/day(농도 감소율)로 보고되어 있으므로 양 기준 식에서는 `Vc`를 곱한다.

### 3.2 대표값 `[확정]` — 출처: Kovalenko P, et al. *CPT Pharmacometrics Syst Pharmacol* 2016;5(11):617-624

| 파라미터 | 값 | 단위 | 비고 |
|---|---|---|---|
| Vc | 2.74 | L | 체중 공변량 기준값 |
| ke | 0.0459 | 1/day | 선형 소실 속도상수 |
| k12 | 0.0652 | 1/day | |
| k21 | 0.129 | 1/day | |
| F | 0.607 | – | 생체이용률 |
| Vmax | 0.968 | mg/L/day | |
| Km | 0.01 | mg/L | 고정(추정 아님) |

- 표·쪽수: 최종 모델 파라미터 표(논문 내 표 번호·쪽수 **확인 필요** — 사용자 제공 요청 중, `config/params_typical.yaml`의 `source` 필드에 기재).
- 유도량: 선형 CL = ke·Vc = 0.1258 L/day, Vp = Vc·k12/k21 = 1.385 L, 포화 시 MM 소실 속도 = Vmax·Vc = 2.65 mg/day.

### 3.3 체중 공변량 `[PENDING]`
체중은 중심구획 용적의 공변량. 형태 `Vc_i = Vc · (WT_i / WT_ref)^θ_WT`로 구현.
`θ_WT`와 `WT_ref`는 문헌값 확인 필요. 개발 중에는 `config/dev_assumptions.yaml`의
`[DEV-가정]` 값을 쓰며 최종 실행에서는 오류로 중단된다.

### 3.4 흡수속도상수 ka `[PENDING → 보정]`
문헌 미보고. 300 mg 단회 피하투여의 **관측 Cmax·tmax를 재현**하도록 보정한다
(`scripts/01_calibrate_ka.R`). 보정 목표값(관측 Cmax, tmax, 출처 표·쪽수)은 사용자 제공 필요.
보정 근거와 손실함수, 결과는 `results/calibration/`과 `DECISIONS.md`에 기록.
목표값 입수 전에는 ka 격자(0.1–0.6 /day)에 대한 민감도만 산출한다.

### 3.5 개체간 변동(IIV)·잔차 오차 `[PENDING]`
FDA Clinical Pharmacology Review, BLA 761055, Table 4.6.6–4.6.7 값을 사용한다.
해당 표는 별도 제공 예정(미제공 상태 — 요청 중). 형태:
- IIV: 로그정규 (`P_i = P · exp(η)`), F는 logit-normal.
- 잔차: 가산 + 비례 결합형 `y = C·(1+ε_p) + ε_a`.
- Km은 기본 고정(IIV 없음). 민감도 시나리오에서 Km IIV 부여(§5).

### 3.6 파라미터 불확실성(바깥층) `[PENDING]`
분산-공분산 행렬이 있으면 사용, 없으면 RSE 기반 대각 근사(로그 척도 정규). RSE 출처 필요.

## 4. 단계 1: 모델 구현과 검증 (가장 중요)

자동 테스트(`tests/testthat/`)로 구현. 전부 통과해야 단계 2 결과 보고 가능.

### 4.1 구조 테스트 (`test-model-structure.R`) — BEmaster·IIV 불필요
- 질량 보존(Vmax=0, ke=0이면 총량 = F·Dose 유지).
- Vmax=0이면 해석적 2구획 1차 흡수 해와 일치.
- Km→0 극한에서 MM 항이 0차 소실로 수렴.
- F가 depot 초기량에 정확히 반영.

### 4.2 Clot 2021 설계 재현 (`test-step1-clot2021.R`)
설계: 300 mg 단회 피하, n=8, 채혈 Day 2·4·8·11·15·22·29·36·43·50·57(투여 Day 1 → 투여 후 1·3·7·10·14·21·28·35·42·49·56일), LLOQ 0.078 mg/L.

| 용량 | 목표 AUClast/AUCinf | 목표 마지막 정량 시점(투여 후 일) |
|---|---|---|
| 300 mg | ≈ 98% | 중앙값 42 (범위 28–49) |
| 200 mg | ≈ 94.6% | 중앙값 28 |
| 600 mg | ≈ 98.1% | 56 |

- 시점 해석: 목표 "42일/28일/56일"은 투여 후 경과일로 해석(= 스터디 Day 43/29/57). 채혈표에 56일이 없으므로 다른 해석은 불가. **사용자 확인 요청.**
- (a) **대표 개체 tlast 검사**: IIV 없이 대표값으로 각 용량의 tlast가 목표 중앙값과 같은지. — 지금 실행 가능. (a')는 ka 격자별 진단 표(단정 없음, D-008).
- (b) **모델 기반 AUC 비율 진단**: ODE 진적분으로 AUC(0–tlast)/AUC(0–∞). 정상 범위(90–100%) 확인과 기록만 하며 NCA 기반 목표와 직접 비교하지 않는다(D-007).
- (c) **NCA 기반 AUClast/AUCinf**: BEmaster로 산출. — BEmaster 입수 후.
- (d) **tlast 분포(중앙값·범위)**: IIV 반영, n=8 반복 추출. — FDA 표 입수 후.

### 4.3 변동성 검사
- Li 2020 Table 3의 300 mg AUClast CV 범위 35–51% 안에 모의 CV가 드는지. — FDA 표 입수 후.
- Cohen 2022의 200 mg AUClast log 척도 SD ≈ 0.49와 비교. — FDA 표 입수 후.
- 각 문헌의 서지 정보·쪽수 `[PENDING]`(사용자 제공).

### 4.4 단계 1 현재 상태 (2026-09-23, dev 모드·자리표시자 ka 0.25, 기준 체중 75 kg)

| 검사 | 상태 | 요약 |
|---|---|---|
| 구조 테스트 7건 | PASS | 해석해 일치, 질량 보존, MM 0차 극한, 다개체 일관성 |
| (a) 대표 개체 tlast | **FAIL(300 mg)** | 200 mg 28일 ✓, 600 mg 56일 ✓, 300 mg 35일(목표 42). 연속시간 LLOQ 교차 37.5일로 목표 창 [42,49)에 4.5일 미달. 200 mg(28.5일)·600 mg(56.1일)도 각 창의 하한 직상 |
| (a') ka 격자 진단 | 기록 | ka 0.15–0.6에서 300 mg은 항상 35일. ka ≥ 0.3이면 600 mg(49일), ka ≥ 0.4이면 200 mg(21일)도 이탈 → ka 보정 목표(Q3) 필요 |
| (b) 참값 AUC 비율 | 기록 | 99.6–100%(NCA 목표와 비교 불가, D-007) |
| (c) NCA 비율 | BLOCKED | BEmaster(Q1) |
| (d) IIV tlast 분포, Li 2020 CV, Cohen 2022 SD | BLOCKED | FDA 표(Q2) + BEmaster(Q1) |

원인 후보와 조치는 DECISIONS D-009. 파라미터 조정은 하지 않았다. 단일 가정 변화로 세 용량을 동시에 맞추는 조건: F ×1.2, Vmax ×0.8, ke ×0.7, 또는 체중 60 kg(θ_WT=1 가정). 결과 표: `results/dev/step1_typical_grid.csv`, `step1_lloq_crossing.csv`, `step1_sensitivity_scan.csv`.

## 5. 단계 2–3: 가상 시험 루프와 시나리오

### 5.1 설계(기본) `[DEV-가정, 확인 필요]`
- 평행군 2 arm(대조 R, 시험 T), 단회 300 mg 피하, arm당 n = `config/trial_design.yaml`.
- 채혈(투여 후 일): 기본 `0, 1, 2, 3, 4, 5, 7, 10, 14, 21, 28, 35, 42, 49, 56` (Day 57 종료).
  비교 일정: Day 85 종료(`… 63, 70, 77, 84`), Day 42–57 밀도 변화(`42, 45, 49, 52, 56` 등).
- 채혈 허용창: 시점별 `±window`(config). 실제 채혈시각 = 계획시각 + U(−w, +w).
- 체중: 포함기준(하한·상한) 안에서 절단 분포, 성별 혼합비 config. 분포 모수 출처 필요.
- LLOQ 0.078 mg/L 미만은 BLQ 검열(값은 NA, 플래그 유지). BLQ 취급 규칙은 BEmaster를 따른다.

### 5.2 2층 몬테카를로
- 바깥층: 파라미터 불확실성 추출(§3.6). 안쪽층: 시험 반복(IIV·잔차·채혈 편차).
- 개발 규모 바깥 20 × 안쪽 50. 최종 규모는 별도 지시.
- 난수: 실행마다 마스터 시드 → 바깥층 시드 → 안쪽층 시드를 결정적으로 파생(`R/seeds.R`), 로그에 기록.

### 5.3 개체 수준 산출(BEmaster)
AUClast, AUCinf, Cmax, 외삽 비율(%AUCextrap), λz 산출 가능 여부, λz, 조정 R².

### 5.4 시험 수준 분석(BEmaster)
로그 변환 후 체중 층을 포함한 분산분석, 기하평균비 90% CI, 80–125% 판정.

### 5.5 시나리오(시험약 arm에만 차이)
| 코드 | 내용 |
|---|---|
| S0 | 차이 없음 |
| S1a / S1b | F −5% / −10% |
| S2 | 선형 소실(ke) −10% |
| S3 | Vmax −10% |
| S4 | Km ×2 |
| K1 | (민감도) Km 상향 — 값 `[PENDING]` |
| K2 | (민감도) Km 개체간 변동 부여 — CV `[PENDING]` |

차이 방향(+/−)은 config에서 지정. 기본은 노출 감소 방향(F↓, CL↑ 계열은 별도 확인).

## 6. BEmaster 연결 규칙

- `R/nca_bemaster.R`, `R/be_bemaster.R`만이 NCA·BE 판정의 진입점이다. 다른 곳에서 AUC·λz·CI를 계산하지 않는다.
- 의존성은 버전 고정(커밋 SHA 또는 릴리스 태그)으로 `renv.lock`에 기록. 복사·수정 금지.
- 기대 인터페이스(확인 필요): 개체별 `time, conc, dose` → `AUClast, AUCinf, Cmax, tmax, lambda_z, r2_adj, n_lambda, pct_extrap, lambda_ok`; 시험별 데이터 → `GMR, CI_lower, CI_upper, pass`.
- BEmaster가 없으면 관련 테스트는 `skip`되고 파이프라인은 NCA 경계에서 중단된다. **모델 기반 진적분 AUC("truth")는 NCA 대체가 아니라 편향 평가용 참값**으로만 쓴다.

## 7. 단계 4 산출물

- 개인 수준: 외삽 비율 분포, λz 산출 불가 비율.
- 시험 수준: 시나리오별 AUClast 판정과 AUCinf 판정의 일치율, 두 지표 log GMR 차이 분포, 공동 1차 조합별 검정력.
- 채혈 일정 비교(Day 57 vs Day 85, Day 42–57 밀도).
- 보고서(`report/report.Rmd`): 목적, 모델과 출처, 가정과 한계(Km 고정으로 말단 산포 과소평가 가능성 포함), 검증 결과, 결과 표·그림, 재현 방법.

## 8. 미결 항목(사용자 입력 필요) — 최신 목록

| # | 항목 | 필요한 것 | 막히는 단계 |
|---|---|---|---|
| Q1 | BEmaster 모듈 | 소스(저장소 URL 또는 파일) + 버전, 함수 인터페이스 | 1(c), 2, 3, 4 |
| Q2 | FDA BLA 761055 Table 4.6.6–4.6.7 | IIV(%CV 또는 ω²), 잔차(가산·비례), RSE, 가능하면 분산-공분산 | 1(d), 4.3, 2 |
| Q3 | ka 보정 목표 | 300 mg 단회 관측 Cmax, tmax(중앙값·범위)와 출처 표·쪽수 | 3.4 |
| Q4 | 체중 공변량 | θ_WT, WT_ref(Kovalenko 2016) | 2 |
| Q5 | 검증 시점 해석 | "42/28/56일" = 투여 후 경과일(Day 43/29/57)로 봐도 되는지 | 1 |
| Q6 | 가상 시험 설계 | arm당 n, 포함기준(체중·BMI), 성별 비, 체중 분포 출처, 채혈창, 체중 층 경계 | 2 |
| Q7 | 문헌 서지 | Clot 2021, Li 2020, Cohen 2022의 정확한 서지·표·쪽수 | 보고서 |
| Q8 | Km 민감도 값 | K1 상향 Km 값, K2 Km IIV CV | 3 |
| Q9 | Kovalenko 2016 표 번호·쪽수 | 대표값 표의 표 번호·쪽수 | 보고서 |
| Q10 | 저장소 기본 브랜치 | `main`이 없어 PR 생성 불가. `main`을 만들거나(빈 커밋 허용 여부) 기본 브랜치를 지정 | PR |
| Q11 | Clot 2021 집단 정보 | 8명의 체중(평균·범위), 성별, 인종 — (a) 미달 원인 판별용 | 1 |

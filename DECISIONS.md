# DECISIONS.md — 결정 기록

형식: `D-번호 | 날짜 | 결정 | 근거 | 영향 파일`. 파라미터·가정 변경은 SPEC.md와 같은 커밋에서 갱신.

## D-001 | 2026-09-23 | 실행 환경: R 4.3.3(apt) + rxode2 5.1.7(CRAN 릴리스, GitHub `cran/*` 미러에서 소스 설치)
- 근거: 이 세션의 클라우드 환경은 CRAN·Posit Package Manager·conda 접근이 정책상 차단(403)되어 있고, Ubuntu apt와 GitHub git 읽기만 가능했다. `cran/<pkg>` 미러의 태그는 CRAN 릴리스와 동일 소스이므로 버전 고정 재현성에 문제 없음. 사용자 환경에서는 `renv::restore()`로 CRAN에서 동일 버전을 받으면 된다.
- 영향: `renv.lock`, `README.md`.
- 보충(같은 날): apt의 Rcpp 1.0.12는 rxode2 5.1.7이 쓰는 가변 인자 `NumericVector::create`를 지원하지 않고, Debian 재패키징 BH는 Boost odeint 헤더가 없어 configure가 중단된다. Rcpp 1.1.2, BH 1.90.0-1, RcppParallel 6.2.1을 CRAN 릴리스에서 설치. RcppParallel은 시스템 TBB를 쓰되 `TBB_INC`에 `/usr/include`를 직접 주면 `#include_next` 체인이 깨지므로 심볼릭 링크 디렉터리(`/opt/tbb-inc`)를 경유.

## D-002 | 2026-09-23 | 모델 식에서 Vmax(mg/L/day)에 Vc를 곱해 양(mg) 기준 소실 속도로 변환
- 근거: Kovalenko 2016은 Vmax를 농도 단위(mg/L/day)로 보고. 상태변수를 양으로 두므로 단위 정합을 위해 `Vmax·C/(Km+C)·Vc`.
- 영향: `R/model.R`, SPEC §3.1.

## D-003 | 2026-09-23 | 검증 목표의 "마지막 정량 시점 42/28/56일"은 투여 후 경과일로 해석(스터디 Day 43/29/57)
- 근거: 채혈표(Day 2…57)에 56일이 없어 스터디 Day 해석이 불가능. 투여 Day 1 기준 경과일이면 세 값 모두 채혈 시점과 일치. 사용자 확인 요청(SPEC Q5).
- 영향: `config/design_clot2021.yaml`, `tests/testthat/test-step1-clot2021.R`.

## D-004 | 2026-09-23 | NCA·BE 판정은 BEmaster 어댑터(`R/nca_bemaster.R`, `R/be_bemaster.R`)만 통해 수행. 자체 NCA 미구현
- 근거: 과제 원칙("비구획분석 규칙을 BEmaster와 다르게 구현" 금지). BEmaster 미입수 상태에서는 해당 테스트를 skip하고 파이프라인을 NCA 경계에서 중단. 모델 기반 진적분 AUC는 "truth" 참값으로만 사용하며 NCA 대체가 아님.
- 영향: SPEC §6.

## D-005 | 2026-09-23 | 출처 미확정 값은 `[PENDING]`으로 두고, 개발 실행에만 `config/dev_assumptions.yaml`의 `[DEV-가정]` 값을 허용. `run_mode = "final"`에서는 PENDING 값이 남아 있으면 즉시 중단
- 근거: "출처 없는 값 사용 금지"와 "파이프라인 개발 진행"의 양립. 최종 결과에 임시값이 섞이는 것을 코드로 차단.
- 영향: `R/params.R`, `config/dev_assumptions.yaml`.

## D-006 | 2026-09-23 | 난수 시드 파생 규칙: master seed → 바깥층 i → 안쪽층 j를 `digest` 기반 결정적 해시로 파생
- 근거: 병렬 실행·부분 재실행에서도 동일 시드 재현. 실행 로그에 master seed와 파생 규칙 버전을 기록.
- 영향: `R/seeds.R`.

## D-007 | 2026-09-23 | 검증 (b) "모델 기반 참값 AUC 비율"은 NCA 기반 목표(98/94.6/98.1%)와 직접 비교하지 않는다
- 근거: 참값 꼬리(ODE 진적분)와 NCA 외삽(Clast/λz, λz는 절벽 이전 구간에서 추정)은 다른 양이다. Km=0.01 mg/L 고정 모델에서 참값 비율은 99.6–100%로 거의 1이고, NCA 외삽은 절벽 이전 기울기를 쓰므로 훨씬 크다. 따라서 (b)는 정상 범위(90–100%) 확인과 기록만 하고, 목표 비교는 BEmaster NCA 기반 (c)에서만 한다.
- 영향: `tests/testthat/test-step1-clot2021.R`, SPEC §4.2.

## D-008 | 2026-09-23 | 검증 (a')는 ka 격자 전체 단정 대신 진단 표로 바꾼다
- 근거: ka는 보정 후 고정되는 값이며, 임의 격자(0.1–0.6/day) 전체에서 tlast가 같아야 한다는 조건은 정의된 검증 기준이 아니다. 격자 표는 ka 보정 목표(Q3)의 중요도를 보여주는 진단으로만 남긴다(ka ≥ 0.3이면 600 mg, ka ≥ 0.4이면 200 mg tlast도 목표에서 벗어남).
- 영향: 같은 파일, SPEC §4.2.

## D-009 | 2026-09-23 | 단계 1 (a) 미달 상태 기록: 300 mg 대표 개체 tlast 35일(목표 중앙값 42일). 파라미터 조정 없이 원인 후보를 보고
- 사실: Kovalenko 2016 대표값 + DEV 기준 체중 75 kg + ka 0.15–0.6/day에서 200 mg(28일), 600 mg(56일)은 일치, 300 mg은 35일. 연속시간 LLOQ 교차는 300 mg 37.5일로 목표 창 [42, 49)에 4.5일 미달. 200 mg 28.5일, 600 mg 56.1일은 각 창의 하한 바로 위(모든 용량이 창의 이른 쪽에 몰려 있어, 체계적으로 노출이 약간 낮거나 소실이 약간 빠른 방향). 노출을 약 15–20% 높이는 단일 가정 변화(F ×1.2, Vmax ×0.8, ke ×0.7, 체중 60 kg[θ_WT=1 가정]) 중 어느 것이든 세 용량을 동시에 맞춘다. LLOQ 변화는 영향 없음(절벽 구조).
- 해석 후보(확정 아님): ① Clot 2021 집단(건강인 8명)의 체중이 기준 체중보다 가벼움(Q4·Q6 필요), ② 건강인은 AD 환자보다 표적 매개 소실(Vmax)이 낮을 수 있음(Kovalenko 집단은 환자 위주), ③ n=8 표본의 중앙값 변동, ④ IIV 반영 시 이산 tlast 중앙값이 대표 개체와 다를 수 있음(Q2 필요).
- 조치: 파라미터를 바꾸지 않는다. Q2(IIV), Q3(ka 목표), Q4(체중 공변량), Clot 2021 집단 체중 정보를 받은 뒤 (a)·(d)를 재평가한다. 그 전까지 단계 2 이후 결과는 보고하지 않는다.
- 영향: `results/dev/step1_*.csv`, SPEC §4.4(신설).

## D-010 | 2026-09-23 | BEmaster에 바인딩하지 않고 NCA와 평행군 통계를 R로 직접 구현 (검토 종합 지시서 §3, Q1 종결)
- 근거: 제공된 BEmaster Analysis v2.4.2(브라우저 JS) 검토 결과 — 선형 사다리꼴만, λz는 마지막 양수 3점 고정 OLS(R² 검정 없음), 마지막 채혈이 BQL이면 AUCinf ≈ AUCt(Clast를 1e-6으로 치환), 외삽률·신뢰 기준 미산출, BE 검정은 대상자별 T/R 쌍이 필요한 쌍체 검정(평행군 불가). 실제 시험(평행군)과 같은 규칙을 쓸 수 없으므로 과제 전제가 성립하지 않는다.
- 결정: `R/nca_bemaster.R`, `R/be_bemaster.R` 삭제. `R/nca.R`(규칙은 `config/nca_rules.yaml`), `R/be_stats.R`(log 변환 pooled t 90% CI, ANCOVA 민감도) 구현. BEmaster 호환 모드(선형 사다리꼴, BQL=0, 마지막 3점 λz)는 Cmax·AUCt 교차검증 옵션으로만 유지. BEmaster 확장은 범위 밖.
- 영향: SPEC §6 전면 교체, D-004 폐기.

## D-011 | 2026-09-23 | IIV·잔차는 Kovalenko 2016 Table 2 값으로 확정; FDA BLA 761055 Table 4.6.6–4.6.7은 이미지라 추출 불가 (Q2 종결)
- 값: ω²(분산) V2 0.0225, ke 0.131, ka 0.251, Vmax 0.0428; k23·k32·F IIV 없음. 잔차 비례 24.2% CV, 가산 0.03 mg/L 고정. 체중 지수 0.705, 기준 75 kg (Q4 종결). ka 0.254/day.
- 구조 민감도 모델(Kovalenko 2020 Model 1, transit 3개)은 IIV 미공개(shrinkage만 보고)이므로 2016 IIV를 차용한다. 2020 모델의 기준 체중은 미기재 → 75 kg 가정(확정 전 가정).
- 영향: `config/params_typical.yaml`, `config/params_variability.yaml`, `config/params_k2020_model1.yaml`.

## D-012 | 2026-09-23 | 바깥층(파라미터 불확실성) 몬테카를로는 실행하지 않음 — 단일층, 시험당 500회
- 근거: 지시서 §5–6은 시험당 500회만 요구하고 RSE·분산-공분산을 제공하지 않는다. 코드에서 바깥층 관련 기능은 제거했다(필요 시 D-006 시드 규칙으로 재도입 가능).
- 영향: `R/mc.R`, `config/params_variability.yaml`(uncertainty.enabled: false).

## D-013 | 2026-09-23 | 공통 난수 설계: 시험 j의 피험자·배정·채혈 편차·잔차는 시나리오와 일정에 무관하게 같은 시드에서 추출
- 근거: 지시서 §6 "대조군은 시나리오 간 공통 난수". 확장하여 시험군도 같은 eta·잔차를 쓰고 고정효과 배율만 바꾼다(시나리오 간 비교의 분산 감소). 모든 일정의 합집합 격자에서 한 번 풀고 일정별 부분집합을 취하므로 일정 비교도 공통 난수다.
- 시드: (master, j, "subj"), (master, j, "alloc"), (master, j, arm, "jitter"), (master, j, arm, "eps"). 개인 수준 모집단: (master, tag, ·).
- 영향: `R/trial.R`, `R/simulate.R`.

## D-014 | 2026-09-23 | 단계 1 판정 정정: "미통과" → "검증 목표 오지정". 원인은 Clot 2021 집단 체중. gate 재정의
- 근거(검토자): 독립 구현(Python, 적응 스텝 RK4)에서도 체중 78 kg 기준 300 mg tlast 중앙값 35일(5–95 백분위 22–50일)로 동일. rxode2 모델 코드는 맞다. Clot 2021 Table 1 체중은 200 mg 62.2 ± 9.0, 300 mg 59.9 ± 8.9, 600 mg 58.3 ± 6.5 kg(Discussion: China 60, Japan 64, US 78 kg).
- D-009의 원인 후보 중 "건강인 대 아토피 환자의 표적 매개 소실 차이"는 삭제한다(Kovalenko 2020 Table 2: population의 Vmax 효과 0.967, 유의하지 않음). 남는 후보는 체중이며 이번 gate가 이를 직접 검사한다.
- 새 검정: 대표 개체 비교 금지. IIV·잔차 포함 8명 코호트 2,000회(체중은 군별 평균·SD 절단 정규, 남 50–100, 여 40–90), 관측 중앙값이 코호트 중앙값 분포의 5–95 백분위 안이면 통과(한 칸 차이는 실패 아님). 정량 gate: 체중 매칭 후 AUClast 평균 ±15%, log-CV 35–51%. Cmax는 gate 제외(모델이 10–20% 높음, 한계로 기재).
- 영향: `tests/testthat/test-step1-clot2021.R`, `scripts/02_validate_step1.R`, `R/step1.R`, `config/design_clot2021.yaml`. D-007·D-008의 진단 테스트는 삭제(대표 개체 비교 자체를 금지).

## D-015 | 2026-09-23 | ka 보정 금지. `scripts/01_calibrate_ka.R`은 보관용(실행 시 중단)
- 근거: 지시서 §1. ka는 Kovalenko 2016 값과 IIV를 그대로 쓴다. Q3 종결.

## D-016 | 2026-09-23 | Day 85 연장안 철회, 목적 재정의: Day 36–50 채혈 밀도의 가치 판단이 1차 목적
- 후보 일정 B0, B−, D1–D4(SPEC §5.2). 판정 규칙은 사전 명시(SPEC §7.3): B0 대비 (a) AUClast CI 폭 상대 감소 ≥ 5%(임계는 확정 전 가정) 또는 (b) AUCinf 신뢰 충족률 +5%p 이상 또는 (c) 외삽 20% 초과 비율의 유의한 감소 중 하나 이상이면 권고, 없으면 B0 유지.
- 영향: `config/trial_design.yaml`(d85 삭제), `config/scenarios.yaml`.

## D-017 | 2026-09-23 | 정리 항목: renv가 시스템 라이브러리를 쓰는 설정(RENV_CONFIG_EXTERNAL_LIBRARIES)은 최종본 전 정리 대상
- 현재 컨테이너는 CRAN 차단으로 시스템 라이브러리에서 실행한다. 최종본에서는 `renv::restore()` 경로만 남기고 README의 우회 설명을 제거한다.

## D-018 | 2026-09-23 | 단계 1 gate 결과: 기본 모델(Kovalenko 2016)은 정량 gate 1항목 미달. 파라미터 조정 없음, 검토자 판정 대기
- 통과: (a) 코호트 중앙값 tlast — 200 mg 28일(모의 5–95% 21–35), 300 mg 42일(31.5–45.5, 관측값과 같은 코호트 26%), 600 mg 56일(49–56). log-CV 35–51% 8/8. 체중 기울기 −40.9 mg·day/L per 5 kg(기대 약 −50). Cohen 2022 log SD 0.444(관측 0.49).
- 미달: AUClast 평균 ±15% — 300·600 mg 5개 데이터셋은 0.94–1.07로 통과, 200 mg ≈80 kg 데이터셋 2개 미달(PKM14271 0.82, Cohen 2022 arm A 0.84; arm B 0.92 통과, 두 arm 평균 대비 0.88).
- 진단(`scripts/02b_step1_diagnostics.R`, `results/step1/diag_*.csv`):
  1. 용량 정규화: 비아시아인 ≈80 kg에서 AUClast/mg의 200 mg/300 mg 비가 관측 0.93(거의 선형), 모의 0.73. 중국인 ≈59 kg 600/300 mg은 관측 1.33, 모의 1.38로 일치. **기본 모델의 저용량·고체중 영역 비선형성이 관측보다 강하다.** Cmax는 200 mg에서 0.92–1.01로 맞으므로 차이는 소실기(대표 개체 200 mg·80 kg은 27.5일에 LLOQ 도달)에 있다.
  2. 확정 전 가정(체중 SD 6–14, 절단 40–150, LLOQ 0.05/0.02, 채혈 일정 3종)은 미달을 해소하지 못함(PKM14271 0.79–0.86, Cohen A 0.82–0.85).
  3. 사전 명시 대안: **Kovalenko 2020 Model 1(구조 민감도)은 8/8 통과**(0.88–1.13, log-CV 35–46%). ω²×1.5는 평균·CV 모두 미달.
  4. 관측 평균 표본오차 맥락(`diag_gate_sampling_error.csv`): 기본 모델 미달은 표본오차로 설명되지 않는다 — Cohen A z = −3.4, PKM14271 z = −2.1 (SE = SD/√n). 2020 모델은 전 데이터셋 |z| ≤ 2.2.
  5. 용량 형태 원인 가정 식별(`diag_dose_shape_summary.csv`, 채택 아님): 검토자가 그럴듯하다고 명시한 범위 안의 단일 변경 — Km 0.005–0.1, Vmax ×0.8/×1.25, MM 소실의 체중 비례 제거 — 중 어느 것도 관측 200/300 mg 비(0.935, 근사 95% 구간 0.76–1.15)를 재현하지 못한다(0.69–0.76). Km은 용량 형태에 거의 영향이 없다(0.73–0.74). Vmax ×0.8이 가장 가깝지만(0.76) 300 mg 비아시아인을 1.19로 밀어 미달시킨다. 따라서 미달 원인은 "Km 고정"이 아니라, 선형 소실 대비 MM 소실의 크기와 연구 간 이질성(다른 연구·분석법·집단에서 온 200 mg 자료) 중 하나로 좁혀지며 현재 자료로는 구분할 수 없다.
  6. 2020 모델 통과의 성격: 2020 모델도 용량 형태 비는 0.74로 같다. 전체 노출이 약 7% 높아 200 mg이 ±15% 안으로 들어오고 300 mg 비아시아인은 1.12(상한 근처)로 올라간 것이다. "2020 모델이 더 맞다"기보다 "두 모델 모두 같은 형태 불일치를 가지며 위치만 다르다".
- 결정: 기본 모델을 임의로 바꾸지 않는다(모델 역할은 지시서가 지정). 2020 모델의 전체 단계 1 결과(`results/step1_k2020/`)와 함께 검토자 판정을 요청한다(`config/gate_decision.yaml`). 판정 전까지 단계 2 이후 결과는 보고하지 않되, 두 모델 모두 계산은 완료해 둔다.

## D-019 | 2026-09-23 | 교차검증 체중 분포 형태: 검토자의 "체중 50–115"는 정규(평균 78, SD 13) 절단 형태로 재현됨
- 균등 50–115에서 AUClast log-CV 49%, 정규(78, 13) 절단 50–115에서 43.2%(검토자 43%). 형태가 전달되지 않아 두 형태로 산출하고 정규 형태를 주 비교로 쓴다(확정 전 가정).
- 밀도 방향 비교는 검토자 정의(Day 46·53 = 경과일 45·52 추가)에 맞춰 교차검증 전용 일정 `XV_B0_dens`, `XV_prev_dens`를 추가했다(D2는 38도 추가하므로 부적합).

## D-020 | 2026-09-23 | 버그 수정: 층화 배정에서 층 경계 밖 체중의 대상자가 배정되지 않던 문제
- 증상: 교차검증(50–115 kg) 시험 수준 CI 폭 25.2%p(기준 17.6), 통과율 과소. 원인: `cut()`이 60–90 밖 체중에 NA 층을 주어 배정 루프에서 제외 → arm당 약 54명만 분석.
- 수정: 층 경계의 바깥 끝을 −Inf/Inf로 연다. 미배정 대상자가 있으면 즉시 오류. 회귀 테스트 추가(`test-population.R`).
- 영향 범위: 기본 설계(절단 60–90)는 영향 없음. 체중 민감도(50–90)와 교차검증만 영향. 수정 전 산출된 시험 수준 결과는 없음(교차검증 1회분 폐기).

## D-021 | 2026-09-23 | YAML 키 `n`은 YAML 1.1에서 불리언으로 해석되므로 `n_subj`로 변경
- 증상: gate 표의 관측 표본 수(n_obs)가 모두 NA. gate 판정 로직은 이 값을 쓰지 않아 판정에는 영향 없음. 저장된 두 gate 표의 n_obs 열은 config 값으로 보정했다.
- 다른 config에 y/n/yes/no/on/off 키가 없음을 확인했다.

## D-022 | 2026-09-23 | 버그 수정: 요약 함수의 data.table 스코프 — 인자명이 열이름과 같아 필터가 무효화됨
- 증상: `summarize_trials(be, method)`의 `be[be$method == method]`에서 `method`가 열로 해석되어 항상 참 → pooled t와 ANCOVA 결과가 섞인 요약. `paired_trial_width_vs_ref`도 같은 이유로 필터가 무효 → 병합 폭증으로 중단.
- 영향: 시험 수준 원자료(`*_be_raw_*.csv`)는 정상. 요약 표만 틀렸으므로 `scripts/14_postprocess.R`로 원자료에서 다시 만들었다(시뮬레이션 재실행 없음). 교차검증은 단일 방법만 써서 영향 없음.
- 수정: 인자를 지역 변수로 옮겨 필터, 중복 검사 추가, 섞인 자료로 회귀 테스트(`test-summarize.R`). 요약 로직을 `R/postprocess.R`로 모아 11·12·14 스크립트가 공유.
- 같은 유형(D-008 이전 ka 인자, 이번 건)이 반복되었으므로 코드 검토 시 data.table `[` 안의 변수명이 열이름과 겹치는지 확인 항목으로 둔다.
